# EchoFrame Trade Journal

A modular trade journaling app with analytics.

## Setup
1. Clone repo
2. Run `npm install`
3. Start backend: `npm run dev`
4. Open `index.html` in browser

## Features
- Log trades with notes
- Archive with filters, sorting, CSV export
- Analytics: equity curve, R distribution, ticker performance, rolling win rate, drawdown
